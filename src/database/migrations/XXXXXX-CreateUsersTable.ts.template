/**
 * Migration Template for Users Table
 * 
 * To create this migration, run:
 * npm run migration:generate -- -n CreateUsersTable
 * 
 * Or manually create the file with the following structure:
 */

import { MigrationInterface, QueryRunner, Table, TableIndex } from 'typeorm';

export class CreateUsersTable1234567890123 implements MigrationInterface {
  public async up(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.createTable(
      new Table({
        name: 'users',
        columns: [
          {
            name: 'user_id',
            type: 'uuid',
            isPrimary: true,
            generationStrategy: 'uuid',
            default: 'uuid_generate_v4()',
          },
          {
            name: 'company_id',
            type: 'uuid',
          },
          {
            name: 'user_name',
            type: 'varchar',
            length: '100',
          },
          {
            name: 'mobile_number',
            type: 'varchar',
            length: '20',
          },
          {
            name: 'employee_code',
            type: 'varchar',
            length: '50',
            isNullable: true,
          },
          {
            name: 'user_role_id',
            type: 'uuid',
            isNullable: true,
          },
          {
            name: 'status',
            type: 'enum',
            enum: ['Draft', 'Active', 'Inactive'],
            default: "'Draft'",
          },
          {
            name: 'password_enabled',
            type: 'boolean',
            default: false,
          },
          {
            name: 'otp_enabled',
            type: 'boolean',
            default: false,
          },
          {
            name: 'created_on',
            type: 'timestamp',
            default: 'CURRENT_TIMESTAMP',
          },
          {
            name: 'modified_on',
            type: 'timestamp',
            default: 'CURRENT_TIMESTAMP',
            onUpdate: 'CURRENT_TIMESTAMP',
          },
          {
            name: 'is_deleted',
            type: 'boolean',
            default: false,
          },
        ],
      }),
      true,
    );

    // Create unique index for company + mobile number
    await queryRunner.createIndex(
      'users',
      new TableIndex({
        name: 'IDX_users_company_mobile',
        columnNames: ['company_id', 'mobile_number'],
        isUnique: true,
      }),
    );

    // Create unique index for company + user name
    await queryRunner.createIndex(
      'users',
      new TableIndex({
        name: 'IDX_users_company_username',
        columnNames: ['company_id', 'user_name'],
        isUnique: true,
      }),
    );
  }

  public async down(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.dropTable('users');
  }
}
